# Built binary. Anchored: an unanchored `dsx` would also hide any file or
# directory of that name at any depth.
/dsx
# `go build -ldflags -X main.version=…` probe, built and removed by CI
/dsx-stamp-probe
# goreleaser output
/dist/
# coverage profile written by the CI test step
/cover.out

# JetBrains
.idea/

.goal-prompt.txt

# agent worktrees
.claude/

# local, gitnexus-managed (see hook)
CLAUDE.local.md
AGENTS.md
.gitnexus/

# live-suite configuration; see .env.example
.env
